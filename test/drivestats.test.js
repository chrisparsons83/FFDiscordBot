const expect = require('chai').expect;
const drivestats = require('../lib/drivestats.js');
const commands = require('../lib/commands.js');

describe('drivestats library and commands', () => {

/*   it('Should complain when string is empty', () => {
    const object = {
      args: ""
    };
    return commands['!drivestats'](object).then(() => {
    }).catch((err) => {
      expect(err).to.be.a.string;
      expect(err).to.equal('I need a team symbol.');
    });
  });

  it('Should complain when there are team symbol is invalid', () => {
    const object = {
      args: "jay"
    };
    return commands['!drivestats'](object).then(() => {
    }).catch((err) => {
      expect(err).to.be.a.string;
      expect(err).to.equal('Invalid team symbol.');
    });
  });

  describe('Drivestats functions', () => {
    const object = {
      args: "GB"
    };
    it('Should return a non-empty embed object.', () => {
      return commands['!drivestats'](object).then((obj) => {
        expect(obj.embed.title).to.be.not.empty;
        expect(obj.embed.description).to.be.not.empty;
        expect(obj.embed.color).to.be.not.empty;
        expect(obj.embed.thumbnail).to.be.not.empty;
        expect(obj.embed.fields).to.be.not.empty;
      });
    });
  });

  describe('Drivestats functions', () => {
    const object = {
      args: "lv"
    };
    it('Should return a non-empty embed object.', () => {
      return commands['!drivestats'](object).then((obj) => {
        expect(obj.embed.title).to.be.not.empty;
        expect(obj.embed.description).to.be.not.empty;
        expect(obj.embed.color).to.be.not.empty;
        expect(obj.embed.thumbnail).to.be.not.empty;
        expect(obj.embed.fields).to.be.not.empty;
      });
    });
  }); */
});
